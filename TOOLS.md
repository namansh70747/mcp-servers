# Tool catalog

Auto-generated by `tests/gen_docs.py`. **42 servers**, **672 tools** total.

## Summary

| Server | #Tools | Purpose |
| --- | ---: | --- |
| [`api-tester`](#api-tester) | 18 | Save & run HTTP requests. add_request, run, run_collection, list_requests, set_env, set_auth, set_assertions, import_curl, import_openapi, history, curl_for. |
| [`apollo`](#apollo) | 9 | Find CTO/CEO/team at a company (free people-search; no emails on free tier). Filter by seniority/title/location, paginate, enrich orgs. Then resolve emails with email-finder.find(). |
| [`blog-drafter`](#blog-drafter) | 13 | Draft posts (draft-only). new_draft/save_draft/list/get/export_md. outline() & outline_to_draft() scaffold; export_platform() formats for dev.to/medium/linkedin/hashnode; set_meta/seo_check/list_series. |
| [`bookmark-vault`](#bookmark-vault) | 24 | Save/search links: add_bookmark, search (incl. archived page text), archive, find_duplicates/dedupe, import_html (browser export), tag/tag_tree, export_csv/html. |
| [`campaign`](#campaign) | 17 | Outreach dedup ledger + suppression. filter_uncontacted(companies) before pitching; record_outreach(...) after sending; add_suppression(...) for do-not-contact. import/export CSV; analytics; per-contact cooldown. |
| [`codeedit`](#codeedit) | 16 | Safe code editing with auto-backup + auto-rollback. Mutating ops: preview_patch, apply_patch, replace_in_file, insert_lines, delete_lines, replace_lines, write_file, multi_edit (atomic), undo, list_backups. Validation (degrade gracefully if tool absent): validate, syntax_check, format_code, lint, run_tests. Every write backs up first and rolls back on a failed syntax check when validate=True. |
| [`codeindex`](#codeindex) | 29 | Project code index. Call index_project(path) once, then relevant_context(task) to pull the right files/symbols, get_lines for exact content, references/definition/outline/imports for navigation. todo_scan/duplicate_code/dead_code_hints surface issues; diff_since(ref) shows what changed. Run export_context_file() to prime every client. Re-run reindex() after edits. |
| [`contacts`](#contacts) | 18 | Local CRM for outreach targets. add_contact/find/update_status; import_csv/export_csv; find_duplicates/merge_contacts/dedupe; tag/enrich_from_signature; stats. |
| [`daily-digest`](#daily-digest) | 5 | Cross-server read-only digest of what needs action today. today() and whats_due() return a ranked, sourced list of items ({source, item, due, urgency}); counts_by_source() summarizes; source(name) drills into one server. Missing source DBs/tables are skipped gracefully. |
| [`deckforge`](#deckforge) | 24 | Build .pptx decks. create_presentation(template=career\|project_pitch) -> add_* slides -> save_presentation. build_from_outline() does it in one call. export_pdf needs LibreOffice. |
| [`devlog`](#devlog) | 11 | Summarize git activity: daily_log, weekly_summary, standup(repo), authors, activity, streak, file_churn, multi_summary, export_markdown, contributors. |
| [`email-finder`](#email-finder) | 13 | Free email resolution. find(name, company, domain) orchestrates everything; guess() for patterns, verify() to check deliverability, from_github() for devs, scrape_site() to harvest published emails, bulk_find() for batches. |
| [`emailcheck`](#emailcheck) | 7 | Vet outreach before sending: validate_email / check_mx (can the domain receive?), spam_score (heuristic content review), extract_emails (pull addresses from text), domain_report (MX+SPF+DMARC). DNS tools need network; the rest are offline. |
| [`expense-tracker`](#expense-tracker) | 7 | Local expense log + monthly budgets (SQLite). add_expense, list_expenses, summary, set_budget, budget_status, delete_expense. |
| [`funding-radar`](#funding-radar) | 12 | Find newly-funded companies (free). scan() pulls RSS + SEC EDGAR + Hacker News; filter_leads()/list_leads() browse results; enrich_targets() adds guessed domains. Feed fresh leads to campaign.filter_uncontacted. |
| [`gitflow`](#gitflow) | 11 | Local git -> PR bridge. status/current_branch/create_branch/stage/commit/push/diff/log on a repo path, then pr_body to synthesize a PR title+markdown from the diff, then open_pr (uses `gh` if installed, else returns a ready-to-run command and the github-server tool to call). All ops are local subprocess git; safe to call read-only ops (status/diff/log) freely. |
| [`github-profile`](#github-profile) | 8 | build_readme(username), suggest_pins, describe_repo, repo_readme, language_stats, contribution_summary, profile_stats — from live GitHub data. |
| [`habit-tracker`](#habit-tracker) | 9 | Habits: add_habit, check_in, streak, today (what's due), habit_report, list_habits. |
| [`homebrew`](#homebrew) | 29 | Manage brew packages: search, info, list, outdated, deps, leaves, services, bundle, doctor, cleanup, install(confirm=True). |
| [`interview-prep`](#interview-prep) | 22 | SRS flashcards (add_card/due_cards/review, optional review_fsrs) + decks, a DSA tracker (add_problem/track_attempt/tag_problem/pattern_coverage), and a mock-interview scheduler. |
| [`jobtrack`](#jobtrack) | 18 | Track job applications: add_application, update_status, list_pipeline, due_followups, add_interview, add_note, export_csv/import_csv, analytics. |
| [`learn-tracker`](#learn-tracker) | 16 | Track learning: add_course, log_progress, log_study (streaks), set_goal/goal_progress, add_resource, mark_for_review/reviews_due, whats_next, generate_plan, report. |
| [`linkedin-optimizer`](#linkedin-optimizer) | 7 | Sharpen LinkedIn copy (draft-only). headline_variants, about_section, experience_bullets, optimize_text, keyword_audit. Uses profile.json. |
| [`mac-control`](#mac-control) | 45 | Control macOS: volume, dark_mode, battery, clipboard, notify, screenshot, windows, wallpaper, caffeinate, lock, eject, spotlight, type_text, shortcuts, system info. |
| [`mailbox`](#mailbox) | 28 | Triage Gmail: list_unread / search -> summarize_thread or thread_actions -> draft_reply (drafts only). needs_reply finds what's waiting; summarize_inbox gives an at-a-glance triage. Label/archive/star/trash + Gmail filters supported. |
| [`mailmerge`](#mailmerge) | 11 | Import (import_csv) -> validate (validate_recipients) -> preview/personalize_check -> dedupe vs your ledger -> throttle_plan -> render_batch, then send via reachout.create_draft per row so caps + the campaign ledger stay authoritative. Never sends directly. |
| [`meeting-prep`](#meeting-prep) | 3 | Assemble a meeting briefing from read-only suite DBs (contacts, reachout, codeindex, notes). build_brief(company?, attendees?, topic?) and quick_brief(text). |
| [`news-radar`](#news-radar) | 6 | Find timely outreach signals for a company (free, no key). scan_company(name) pulls Google News + Hacker News; classify hiring/funding/launch/exec triggers. list_signals()/top_signals() browse them. |
| [`notes`](#notes) | 31 | Markdown notes w/ [[backlinks]], #tags, daily notes, templates + FTS. new_note, edit_note, search, get, backlinks, graph, export_note/export_all, daily_note. |
| [`pitchbuilder`](#pitchbuilder) | 18 | Store a tailored project pitch per company and render it into your template. save_pitch -> render_into_template -> (optional) make_onepager -> onepager_to_pdf. suggest_angles for ideas; save_variant for A/B; save_template for a reusable library. |
| [`portfolio-site`](#portfolio-site) | 5 | build_site(theme, github_username, out_dir) from profile.json; set_theme; list_themes; projects_from_github; deploy_instructions. |
| [`project-memory`](#project-memory) | 21 | Durable project memory. At session start call resume() to rehydrate the working thread; remember() decisions/conventions as you make them; pin() the load-bearing ones; link() related memories and graph() to traverse; checkpoint() at milestones so context survives compaction. export_digest() writes a committable MEMORY.md. |
| [`reachout`](#reachout) | 34 | Templated Gmail outreach, drafts-first. render_template -> create_draft (review) -> send_draft. Tracks everything; list_followups_due for follow-ups; sequences + scheduled sends + A/B + analytics. |
| [`readme-changelog`](#readme-changelog) | 9 | gen_changelog/release_notes from git; gen_readme from structure; suggest_bump, lint_commits, keep_a_changelog, contributors, badges. |
| [`recipes`](#recipes) | 14 | Playbooks that RETURN an ordered step plan ({server,tool,args,why}) for the agent to run across the other servers. list_recipes, weekly_outreach, prep_for_company, apply_to_job, daily_briefing, ship_project, cross_search. |
| [`repo-health`](#repo-health) | 8 | Read-only repo hygiene: health_report, stale_branches, large_files, missing_files, todo_census, commit_cadence, gitignore_check. |
| [`resume-forge`](#resume-forge) | 13 | Build tailored résumés/cover letters from profile.json. read_profile -> ats_score(jd) -> build_resume(template) / cover_letter(...) -> export. JSON Resume import/export. PDF needs LibreOffice. |
| [`rss-reader`](#rss-reader) | 8 | Local RSS/Atom reader (SQLite). add_feed, list_feeds, refresh (needs network), unread, mark_read, search, remove_feed. |
| [`scaffold`](#scaffold) | 11 | new_project(stack, name, dest); list_templates; list_licenses; add_ci; add_license; add_precommit; add_devcontainer; add_makefile; git_init. |
| [`snippet-vault`](#snippet-vault) | 14 | Save/search reusable code: save_snippet, search, get, list_by_lang, update_snippet, import_file/import_dir, export, run_snippet, stats, tags. |
| [`task-manager`](#task-manager) | 26 | Todos: add_task (natural-language due ok), complete, list_due/list_tasks, subtasks, projects/tags, recurring tasks, eisenhower(), export_ics. calendar_payload(id) -> google_workspace.create_event. |
| [`time-tracker`](#time-tracker) | 24 | Track time: start(label)/stop(), log_block, pomodoro, set_time_goal/goal_status, daily_report/report/weekly_report, export_csv. |

## Servers

### api-tester

_Save & run HTTP requests. add_request, run, run_collection, list_requests, set_env, set_auth, set_assertions, import_curl, import_openapi, history, curl_for._

- `add_request(name, url, method?, headers?, body?, collection?, auth_type?, auth_value?, assertions?)` — Save a request into a collection. Optional auth (bearer/basic/apikey) and assertions.
- `curl_for(request_id)` — Emit an equivalent curl command for a saved request (round-trips with import_curl).
- `delete_env(env, key?)` — Delete one variable, or the whole environment when key is empty.
- `delete_request(request_id)` — Delete a saved request.
- `get_env(env?)` — List variables for an environment (values returned as-is; treat as local secrets).
- `health()` — Liveness check: confirm this server is running and reachable.
- `history(request_id?, limit?)` — Recent run history — all requests, or just one when request_id is given.
- `import_curl(command, name?, collection?, save?)` — Parse a curl command into a request. Saves it by default; set save=False to just preview.
- `import_openapi(spec, collection?, limit?)` — Import an OpenAPI/Swagger JSON spec (file path or URL) — creates a saved request per operation.
- `list_envs()` — List all environment names that have variables.
- `list_requests(collection?)` — List saved requests.
- `run(request_id, env?)` — Run a saved request (with env-var substitution, auth, and assertions) and record history.
- `run_collection(collection?, env?)` — Run every request in a collection (with the given environment).
- `run_inline(url, method?, headers?, body?, env?, auth_type?, auth_value?, assertions?)` — Run an ad-hoc request without saving it (supports env/auth/assertions).
- `save_response(request_id, path, env?)` — Run a saved request and write its full response body to a file.
- `set_assertions(request_id, assertions)` — Attach response assertions to a saved request.
- `set_auth(request_id, auth_type, auth_value)` — Attach auth to a saved request. auth_type: bearer | basic (user:pass) | apikey (Header:value).
- `set_env(env, key, value)` — Set an environment variable for {{KEY}} substitution. Stored locally in plaintext SQLite.

### apollo

_Find CTO/CEO/team at a company (free people-search; no emails on free tier). Filter by seniority/title/location, paginate, enrich orgs. Then resolve emails with email-finder.find()._

- `enrich_org(domain)` — Enrich a single organization by domain (richer firmographics than find_company): industry,
- `enrich_person(name?, domain?, linkedin_url?)` — Match a single person by name+domain (or LinkedIn URL). Free tier: no email returned — use the
- `find_company(domain?, name?)` — Look up an organization by domain or name (industry, size, funding fields).
- `find_people(domain, titles?, limit?, seniorities?, locations?, page?)` — Find people at a company domain by title (default CEO/CTO/founder). Free tier: no emails —
- `find_people_paged(domain, titles?, per_page?, max_pages?, seniorities?)` — Auto-paginate find_people across up to max_pages, deduping by name+title. Use when you want
- `has_key()` — Introspection: whether APOLLO_API_KEY is configured (so the agent can choose its path).
- `health()` — Liveness check: confirm this server is running and reachable.
- `seniorities()` — List the valid Apollo seniority filter values (offline; no key needed).
- `titles_catalog()` — Common decision-maker title presets you can pass to find_people (offline; no key needed).

### blog-drafter

_Draft posts (draft-only). new_draft/save_draft/list/get/export_md. outline() & outline_to_draft() scaffold; export_platform() formats for dev.to/medium/linkedin/hashnode; set_meta/seo_check/list_series._

- `export_md(draft_id, with_frontmatter?)` — Write a draft to a .md file (you publish manually). with_frontmatter=True prepends a YAML
- `export_md_all(kind?)` — Export every draft (optionally filtered by kind) to .md files; returns the paths.
- `export_platform(draft_id, platform)` — Format and export a draft for a publishing platform ∈ {devto, hashnode, medium, linkedin}.
- `get(draft_id)` — Get a full draft.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_drafts(kind?, limit?)` — List drafts.
- `list_series()` — List drafts grouped by series, parts ordered.
- `new_draft(title, body?, kind?, topic?, author?)` — Create a draft. author defaults to your name/headline from profile.json when omitted
- `outline(topic, kind?)` — Suggest a post structure to fill in. kind ∈ {blog, linkedin, tutorial, thread, newsletter,
- `outline_to_draft(topic, kind?, title?, author?)` — Turn the outline() structure into a draft body scaffold (section headers as markdown), create
- `save_draft(draft_id, body, status?)` — Update a draft's body/status.
- `seo_check(draft_id)` — Analyze a draft for SEO/readability: title length, headings, word count, reading time,
- `set_meta(draft_id, tags?, series?, series_part?, canonical_url?, cover_image?, platform?)` — Set a draft's publishing metadata (tags, series + part, canonical URL, cover image, platform).

### bookmark-vault

_Save/search links: add_bookmark, search (incl. archived page text), archive, find_duplicates/dedupe, import_html (browser export), tag/tag_tree, export_csv/html._

- `add_bookmark(url, title?, tags?, notes?, fetch_title?, archive?)` — Save a bookmark. If no title and fetch_title=True, fetches the page <title>. If archive=True,
- `archive(bookmark_id?, url?)` — Fetch & store a readability text snapshot of a bookmark (by id) or a URL (saved if new), so its
- `bookmarks_by_tag(tag, recursive?)` — Bookmarks under a tag. recursive=True also matches child tags ('parent' matches 'parent/child').
- `dedupe(apply?)` — Report (apply=False) or remove (apply=True) duplicates, keeping the oldest of each group.
- `delete_bookmark(bookmark_id)` — Delete a bookmark and its archived content.
- `domains(limit?)` — Bookmark counts grouped by host, descending — see where your reading concentrates.
- `export_csv(path?)` — Export bookmarks to CSV (returns text; also writes to file if path given or default exports dir).
- `export_html(path?)` — Export bookmarks in Netscape format (re-importable into any browser).
- `find_duplicates()` — Find groups of bookmarks that normalize to the same URL (ignoring scheme/www/utm/trailing slash).
- `get_archive(bookmark_id, max_chars?)` — Return the archived page text for a bookmark.
- `get_bookmark(bookmark_id)` — Get a single bookmark with all fields (archive text truncated).
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_html(path, tags?)` — Import a Netscape-format browser bookmarks export (Chrome/Firefox/Safari 'Export Bookmarks').
- `import_json(path, tags?)` — Import a Chrome 'Bookmarks' JSON file (the one in your Chrome profile dir).
- `list_bookmarks(limit?, tag?)` — List recent bookmarks, optionally filtered by tag (matches hierarchy prefixes).
- `random_bookmark(tag?)` — Surface a random saved bookmark (optionally within a tag) for rediscovery.
- `recent(days?, limit?)` — Bookmarks added within the last N days, most recent first.
- `refresh_titles(limit?)` — Backfill titles for bookmarks missing them (network; safe on failure).
- `search(query, limit?, content?)` — Full-text search bookmarks (title/url/tags/notes). If content=True, also searches archived page text.
- `stats()` — Counts: bookmarks, archived, tags, duplicate groups.
- `tag(bookmark_id, tags)` — Set tags on a bookmark (comma-separated). Supports hierarchy via 'parent/child'.
- `tag_tree()` — Return tags arranged into a hierarchy from 'parent/child' tag paths, with counts.
- `untagged(limit?)` — Bookmarks that have no tags yet (candidates for organizing).
- `update_bookmark(bookmark_id, title?, notes?, tags?)` — Update a bookmark's title/notes/tags (non-empty args only).

### campaign

_Outreach dedup ledger + suppression. filter_uncontacted(companies) before pitching; record_outreach(...) after sending; add_suppression(...) for do-not-contact. import/export CSV; analytics; per-contact cooldown._

- `add_suppression(value, reason?, kind?)` — Add a domain/email/company to the permanent do-not-contact list.
- `analytics()` — Richer rollup: avg sends/company, top companies, suppression by kind, contacts logged, last run.
- `bulk_add_suppression(values, kind?, reason?)` — Add many values to the do-not-contact list in one call. Returns how many were newly added.
- `campaign_stats()` — Totals: companies contacted, suppressions, runs.
- `contact_cooldown(email)` — When was this specific person last contacted, and how many times? From the per-contact log.
- `export_ledger_csv(path?)` — Export the contacted-companies ledger to a CSV file. Returns the path + count.
- `export_suppression_csv(path?)` — Export the do-not-contact list to a CSV file. Returns the path + count.
- `filter_uncontacted(companies, respect_cooldown?)` — Given candidate companies [{company, domain}], return only those NOT already contacted
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_suppression_csv(path, kind?, reason?)` — Bulk-import a do-not-contact list from a file. Accepts CSV (uses 'value'/'domain'/'email'/
- `is_contacted(company?, domain?, email?)` — Check whether a company (by name/domain) has already been contacted, or is suppressed.
- `list_suppressions(limit?)` — List the do-not-contact entries.
- `next_run_due(interval_days?)` — Is the next scheduled outreach run due? Compares now to the last recorded run.
- `recent_outreach(days?, limit?)` — Recent per-contact sends from the outreach log (last N days).
- `record_outreach(company, domain?, contacts?, email?, contact_name?, cooldown_days?, gmail_message_id?, thread_id?)` — Mark a company as contacted (so it's never targeted again). `contacts` = who you emailed.
- `record_run(note?)` — Record that an outreach run started now (for scheduling bookkeeping).
- `remove_suppression(value)` — Remove a value from the do-not-contact list (un-suppress).

### codeedit

_Safe code editing with auto-backup + auto-rollback. Mutating ops: preview_patch, apply_patch, replace_in_file, insert_lines, delete_lines, replace_lines, write_file, multi_edit (atomic), undo, list_backups. Validation (degrade gracefully if tool absent): validate, syntax_check, format_code, lint, run_tests. Every write backs up first and rolls back on a failed syntax check when validate=True._

- `apply_patch(unified_diff, repo?, validate?)` — Apply a unified diff. Uses `git apply` when available, else a pure-python applier.
- `delete_lines(path, start, end, repo?, validate?)` — Delete 1-based lines [start, end] inclusive. Auto-backup; auto-rollback on syntax failure.
- `format_code(path, write?, repo?)` — Format a file with the right tool if installed: black/ruff (.py), prettier (.js/.ts/.json/
- `health()` — Liveness check: confirm this server is running and reachable.
- `insert_lines(path, lineno, text, repo?, validate?)` — Insert `text` BEFORE 1-based `lineno` (lineno > len -> append at end). Auto-backup +
- `lint(path, repo?)` — Lint a file with the first available linter: ruff/flake8 (.py), eslint (.js/.ts),
- `list_backups(path, repo?)` — List available backups for `path`, newest last.
- `multi_edit(edits, repo?, validate?)` — Apply many edits ATOMICALLY: back up all targets, apply all, validate all; if ANY edit or
- `preview_patch(path_or_repo, unified_diff)` — Dry-run a unified diff (NO write). Shows the resulting content + whether it applies cleanly.
- `replace_in_file(path, find, replace, count?, anchor?, repo?, validate?)` — Replace `find` with `replace` in a file. count=0 -> all; anchor restricts replacement to
- `replace_lines(path, start, end, text, repo?, validate?)` — Replace 1-based lines [start, end] inclusive with `text`. Auto-backup; auto-rollback.
- `run_tests(repo, pattern?, timeout_s?)` — Auto-detect and run a project's test suite: pytest (Python), jest/npm test (JS),
- `syntax_check(path, repo?)` — Syntax-check a file by extension: py_compile (.py), node --check (.js/.ts), gofmt (.go),
- `undo(path, repo?)` — Restore the latest backup for `path` (most recent mutating op). Backs up the current
- `validate(path, repo?)` — Run the appropriate syntax check for `path` (alias surface for syntax_check).
- `write_file(path, content, repo?, validate?)` — Write `content` to `path` (create or overwrite). Auto-backup of any existing file;

### codeindex

_Project code index. Call index_project(path) once, then relevant_context(task) to pull the right files/symbols, get_lines for exact content, references/definition/outline/imports for navigation. todo_scan/duplicate_code/dead_code_hints surface issues; diff_since(ref) shows what changed. Run export_context_file() to prime every client. Re-run reindex() after edits._

- `call_graph(name, project?, depth?, limit?)` — Shallow call graph for a function symbol: callers (functions whose body calls `name`) and
- `callers(name, project?, limit?)` — Find call sites of `name` (references that look like `name(`).
- `changed_files(ref?, project?)` — List files changed since a git ref (name + status), using the working tree. Requires the
- `dead_code_hints(project?, limit?)` — Heuristic dead-code hints: defined functions/classes whose name has no reference outside
- `definition(name, project?)` — Where is `name` defined? Returns file + line for each matching symbol.
- `diff_since(ref?, project?, max_files?)` — What changed since a git ref: name-status list, a --stat summary, and (per changed file)
- `duplicate_code(project?, min_lines?, limit?)` — Cheap structural duplicate detector: hash sliding windows of normalized (whitespace-
- `embed_index(project?, window?)` — Build a LOCAL semantic index: chunk each file into ~`window`-line windows and store an
- `export_context_file(project?, write?, extra_targets?)` — Write a compact project digest into auto-load files so every client starts familiar with
- `file_summary(path, project?)` — Get a file's cached summary (and whether it's stale vs the current content hash).
- `get_file(path, project?)` — Return a file's full stored content.
- `get_lines(path, start, end, project?)` — Return lines [start, end] (1-based, inclusive) of a file.
- `grep(pattern, project?, regex?, limit?, ignore_case?)` — Regex (or literal) search across stored file contents with file:line:snippet. Complements
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_graph(project?, limit?)` — Project import graph: file→file edges (resolved internal imports) plus the most-imported
- `imported_by(path, project?)` — Reverse import edges: which indexed files import `path`.
- `imports(path, project?)` — What does this file import? Returns each import target, its kind, line, and the resolved
- `index_project(path, max_file_kb?, respect_gitignore?)` — Index a whole project: store every file's content + symbols + imports. Sets it as the
- `outline(path, project?)` — Hierarchical symbol outline for a file: each symbol with its kind and line range, nested
- `project_map(project?, limit?)` — Tree of files with a one-line summary (cached or heuristic) + symbol count each.
- `references(name, project?, limit?)` — Find references to `name` across the project (word-boundary match), with line snippets.
- `reindex(path?, project?)` — Incrementally refresh the index. With `path` (a single file) update just it; otherwise
- `relevant_context(task, project?, max_files?, budget_chars?, mode?)` — Curated context bundle for a task: top matching files with summary, key symbols, and a
- `search(query, project?, limit?)` — Full-text search file contents + summaries (FTS5), best matches first.
- `semantic_search(query, project?, k?)` — Local semantic search over embedded chunks (run embed_index first). Ranks by cosine
- `set_summary(path, summary, project?)` — Cache an agent-written one/two-line summary for a file (refreshed when its hash changes).
- `status(project?)` — Index stats for the active (or given) project.
- `symbols(path, project?)` — List functions/classes/symbols defined in a file (with line numbers).
- `todo_scan(project?, markers?, limit?)` — Scan the project for TODO/FIXME/HACK/XXX/BUG (and custom markers) comments, returning

### contacts

_Local CRM for outreach targets. add_contact/find/update_status; import_csv/export_csv; find_duplicates/merge_contacts/dedupe; tag/enrich_from_signature; stats._

- `add_contact(name, company, email?, role?, title?, domain?, linkedin?, github?, source?, confidence?, notes?, phone?, twitter?, tags?)` — Add or update a contact (unique by email+company). Validates email; derives domain from email.
- `dedupe(dry_run?)` — Auto-merge obvious duplicate contacts that share the same email. dry_run reports the plan only.
- `enrich_domain(contact_id)` — Fill in a contact's domain from its email if missing. Pure string derivation.
- `enrich_from_signature(contact_id, signature)` — Parse an email signature block for email/phone/title/linkedin/github/twitter and merge in.
- `export_csv(path?, status?, company?)` — Export contacts to a CSV file (optionally filtered by status/company). Returns the path + count.
- `find(query?, company?, status?, tag?, limit?)` — Search contacts by free text (name/email/role/title) and/or company/status/tag filters.
- `find_duplicates()` — Find likely duplicate contacts grouped by shared email (case-insensitive) or name+company.
- `get(contact_id)` — Get a full contact record.
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_csv(path, default_company?)` — Bulk import contacts from a CSV (stdlib csv). Flexible headers: name,email,company,role,title,
- `link_jobtrack_payload(contact_id)` — Return a payload ready to pass to jobtrack.add_application (company/role). Loose cross-server link.
- `list_contacts(limit?)` — List recent contacts.
- `mark_contacted(contact_id)` — Mark a contact as contacted now (sets status + last_contacted_at). Pairs with reachout/campaign.
- `merge_contacts(keep_id, dup_id)` — Merge dup_id into keep_id: fill empty fields from dup, union tags+notes, then delete dup.
- `stats()` — CRM totals: by status, by confidence, by source, plus total + recently added (7d).
- `tag(contact_id, tags)` — Add comma-separated tags to a contact (union with existing, case-insensitive).
- `untag(contact_id, tags)` — Remove comma-separated tags from a contact (case-insensitive).
- `update_status(contact_id, status, notes?)` — Set a contact's pipeline status (new/queued/contacted/replied/bounced/closed).

### daily-digest

_Cross-server read-only digest of what needs action today. today() and whats_due() return a ranked, sourced list of items ({source, item, due, urgency}); counts_by_source() summarizes; source(name) drills into one server. Missing source DBs/tables are skipped gracefully._

- `counts_by_source()` — Per-source counts of how many items currently need action (read-only).
- `health()` — Liveness check: confirm this server is running and reachable.
- `source(name, limit?)` — Drill into a single source server's due items (read-only).
- `today()` — Aggregate everything that needs action today across all suite servers.
- `whats_due(limit?)` — Ranked, sourced list of action items due now, capped at `limit`.

### deckforge

_Build .pptx decks. create_presentation(template=career|project_pitch) -> add_* slides -> save_presentation. build_from_outline() does it in one call. export_pdf needs LibreOffice._

- `add_agenda_slide(deck_id, items, title?, notes?)` — Add a numbered agenda / outline slide.
- `add_bullet_slide(deck_id, title, bullets, levels?, notes?)` — Add a slide with a title and bullet points (optional per-bullet indent levels, speaker notes).
- `add_chart_slide(deck_id, title, categories, series, chart_type?, notes?)` — Add a native chart slide. series=[{name, values:[..]}], chart_type ∈ {bar,column,line,pie,area,doughnut}.
- `add_code_slide(deck_id, title, code, language?, notes?)` — Add a slide with a monospace code block on a panel (whitespace preserved, no wrap).
- `add_comparison_slide(deck_id, title, left_title, left, right_title, right, notes?)` — Add a comparison slide: two titled, panelled columns (e.g. Before vs After, Us vs Them).
- `add_image_slide(deck_id, image_path, title?, caption?, notes?)` — Add a slide with an image (PNG/JPEG). Validates the file exists.
- `add_metrics_slide(deck_id, title, metrics, notes?)` — Add a KPI / big-number slide. metrics=[{value, label}] rendered as cards (up to 4).
- `add_quote_slide(deck_id, quote, attribution?, notes?)` — Add a large centered pull-quote slide with optional attribution.
- `add_section(deck_id, section_title, notes?)` — Add a section-divider slide.
- `add_timeline_slide(deck_id, title, milestones, notes?)` — Add a horizontal timeline. milestones=[{when, what}] rendered as connected panels.
- `add_title_slide(deck_id, title, subtitle?, notes?)` — Add a title slide (optional speaker notes).
- `add_two_column_slide(deck_id, title, left, right, notes?)` — Add a two-column slide (e.g. skills vs experience).
- `build_from_outline(outline, theme?, title?, filename?)` — One-shot deck build. outline=[{type, ...}] where type ∈ {title, bullet, two_column,
- `create_presentation(title?, template?, theme?)` — Start a deck. template ∈ {career, project_pitch, talk, case_study, ''}. Returns a deck_id for subsequent calls.
- `deck_info(deck_id)` — Summarize a deck: slide count and per-slide type/title.
- `delete_deck(deck_id)` — Drop an in-memory deck.
- `export_pdf(pptx_path?, deck_id?)` — Convert a .pptx to PDF via LibreOffice (free). Pass a saved pptx_path, OR a deck_id (saved
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_decks()` — List in-memory decks (id, theme, slide count).
- `list_slide_types()` — List slide types usable in build_from_outline / add_* tools.
- `list_templates()` — Describe the built-in deck blueprints.
- `list_themes()` — List available color themes.
- `save_presentation(deck_id, filename?)` — Write the deck to a .pptx file and return its absolute path.
- `set_speaker_notes(deck_id, slide_index, notes)` — Set (or replace) the speaker notes on an existing slide by index.

### devlog

_Summarize git activity: daily_log, weekly_summary, standup(repo), authors, activity, streak, file_churn, multi_summary, export_markdown, contributors._

- `activity(repo, days?)` — Per-day commit counts over a window (a simple contribution calendar).
- `authors(repo, since?)` — Per-author leaderboard: commits + lines added/removed over a window.
- `contributors(repo)` — All-time contributors with commit counts and first/last commit dates.
- `daily_log(repo, days?, author?)` — Commits in the last N days (optionally filtered by author).
- `export_markdown(repo, days?, author?)` — Paste-ready markdown weekly update: commits grouped by day.
- `file_churn(repo, since?, top?)` — Most-changed files (hotspots) over a window, by total lines added+removed.
- `health()` — Liveness check: confirm this server is running and reachable.
- `multi_summary(repos, days?, author?)` — Roll up commit activity across several repos for the last N days.
- `standup(repo, author?)` — Standup helper: yesterday's commits ('did'), uncommitted changes ('in progress').
- `streak(repo, author?)` — Current and longest consecutive-day commit streaks (last year of history).
- `weekly_summary(repo, author?)` — Last 7 days of commits, plus parsed files/insertions/deletions — raw material for a weekly update.

### email-finder

_Free email resolution. find(name, company, domain) orchestrates everything; guess() for patterns, verify() to check deliverability, from_github() for devs, scrape_site() to harvest published emails, bulk_find() for batches._

- `bulk_find(people)` — Resolve emails for a batch. Each item: {name, domain?, company?, github?}. Reuses the cache.
- `cache_stats()` — Stats on the verification cache and recorded found-emails.
- `clear_cache()` — Clear the verification cache (does not touch recorded found-emails).
- `confidence_breakdown(email)` — Explain the verification signals for an email without short-circuiting: returns each check
- `find(name, company?, domain?, github?, scrape?)` — Orchestrate the full free flow: site scrape -> GitHub commits -> pattern guesses -> verify ->
- `from_github(username, max_events?)` — Find a developer's email from their public GitHub commit history (events API + commit
- `guess(name, domain)` — Generate ranked likely email patterns for a person at a domain (most common first).
- `health()` — Liveness check: confirm this server is running and reachable.
- `hunter_domain_search(domain, limit?)` — Hunter.io free-tier domain search (HUNTER_API_KEY required). Returns known emails + the
- `mx(domain)` — Look up a domain's MX records (tells you the mail host / whether email is even possible).
- `persist_to_contacts(name, company?, domain?, github?, role?, email?, scrape?, notes?)` — Resolve a person's work email AND write it to the shared contacts CRM
- `scrape_site(domain, max_pages?)` — Scrape a company's homepage + about/team/contact/people pages for published emails (mailto +
- `verify(email, check_smtp?, use_cache?)` — Verify an email: syntax + MX, role/disposable flags, catch-all + self-hosted SMTP probe, and

### emailcheck

_Vet outreach before sending: validate_email / check_mx (can the domain receive?), spam_score (heuristic content review), extract_emails (pull addresses from text), domain_report (MX+SPF+DMARC). DNS tools need network; the rest are offline._

- `check_mx(domain)` — Look up a domain's MX records (can it receive mail?). Needs network + dnspython.
- `domain_report(domain)` — Deliverability snapshot for a domain: MX present, SPF record, DMARC policy. Needs network.
- `extract_emails(text, validate?)` — Pull all email addresses out of free text, de-duplicated. Optionally drops syntactically invalid
- `health()` — Liveness check: confirm this server is running and reachable.
- `parse_unsubscribe(content)` — Extract unsubscribe info from raw headers and/or HTML: List-Unsubscribe header value(s),
- `spam_score(subject, body)` — Heuristic spam-likelihood score (0-100, higher = worse) for a subject+body, with itemized reasons
- `validate_email(email)` — Validate an email's syntax (offline) and return its normalized form + domain. Uses email-validator

### expense-tracker

_Local expense log + monthly budgets (SQLite). add_expense, list_expenses, summary, set_budget, budget_status, delete_expense._

- `add_expense(amount, category, note?, date?)` — Record an expense. amount (>0), category, optional note, optional date (YYYY-MM-DD, defaults today).
- `budget_status(month?)` — Show each budgeted category's spend vs. budget for a month (default current), with remaining
- `delete_expense(id)` — Delete an expense by id.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_expenses(month?, category?)` — List expenses, optionally filtered by month (YYYY-MM) and/or category. Newest first.
- `set_budget(category, monthly)` — Set (or update) a monthly budget for a category.
- `summary(month?)` — Monthly summary: total spend and per-category breakdown for a month (default current).

### funding-radar

_Find newly-funded companies (free). scan() pulls RSS + SEC EDGAR + Hacker News; filter_leads()/list_leads() browse results; enrich_targets() adds guessed domains. Feed fresh leads to campaign.filter_uncontacted._

- `as_targets(limit?)` — Return recent leads as targets to pass to campaign.filter_uncontacted. Each item carries
- `dedupe()` — Collapse near-duplicate company rows (same normalized name) keeping the richest record.
- `enrich_targets(limit?, check_dns?)` — Like as_targets but fills a best-effort guessed domain for each lead (optionally DNS-verified).
- `filter_leads(sector?, round?, min_amount_usd?, source?, since_days?, limit?)` — Rich filter over stored leads by sector, round, minimum USD amount, source, and freshness.
- `guess_domain(company, check_dns?)` — Heuristically guess a company's domain from its name. With check_dns=True, probe candidate
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_leads(limit?, source?)` — Browse stored leads (most recent first).
- `scan(source?, limit?, since_days?, query?)` — Fetch recent funded companies and store them (deduped). Returns new leads.
- `scan_hackernews(limit?, since_days?)` — Scan the Hacker News Algolia API (free, no key) for recent funding stories. Stores + returns.
- `scan_rss(feeds?, limit?)` — Pull funding leads from any list of RSS feeds (defaults to the curated free funding set).
- `sources()` — Introspection: list available scan sources and the configured default RSS feeds.
- `stats()` — Summary stats: total leads, breakdown by source/sector/round, and freshness counts.

### gitflow

_Local git -> PR bridge. status/current_branch/create_branch/stage/commit/push/diff/log on a repo path, then pr_body to synthesize a PR title+markdown from the diff, then open_pr (uses `gh` if installed, else returns a ready-to-run command and the github-server tool to call). All ops are local subprocess git; safe to call read-only ops (status/diff/log) freely._

- `commit(repo?, message?, all?)` — Create a commit with `message`. Set `all=True` to auto-stage tracked modifications.
- `create_branch(repo?, name?, from_ref?)` — Create and check out a new branch `name`, optionally based on `from_ref`.
- `current_branch(repo?)` — Return the name of the currently checked-out branch (or detached HEAD info).
- `diff(repo?, from_ref?, to_ref?, staged?)` — Show a diff. With both refs -> `from..to`; with `staged=True` -> index vs HEAD;
- `health()` — Liveness check: confirm this server is running and reachable.
- `log(repo?, n?)` — Return the latest `n` commits (sha, short sha, author, date, subject). Read-only.
- `open_pr(repo?, title?, body?, base?, head?)` — Open a GitHub pull request. Uses `gh pr create` when the GitHub CLI is installed;
- `pr_body(repo?, from_ref?, to_ref?)` — Synthesize a PR title + markdown description from the diff between `from_ref` and
- `push(repo?, branch?, remote?, set_upstream?)` — Push `branch` (default: current) to `remote`. Sets upstream on first push.
- `stage(repo?, paths?)` — Stage changes. `paths` selects files (default: all changes, including new/deleted).
- `status(repo?)` — Working-tree status of a repo: current branch, ahead/behind, and changed files.

### github-profile

_build_readme(username), suggest_pins, describe_repo, repo_readme, language_stats, contribution_summary, profile_stats — from live GitHub data._

- `build_readme(username, write?, include_stats?, include_badges?)` — Draft a GitHub profile README from profile.json + your top repos. include_stats adds a
- `contribution_summary(username)` — Summarize a user's public footprint: followers, public repos, total stars, most-starred and
- `describe_repo(full_name)` — Repo metadata (description, stars, language, topics) to help write a good blurb.
- `health()` — Liveness check: confirm this server is running and reachable.
- `language_stats(username, top?)` — Aggregate primary languages across a user's non-fork repos, with percentages.
- `profile_stats(username)` — Combined dashboard: user basics + language_stats + top repos in one call.
- `repo_readme(full_name, write?)` — Draft a project README.md from repo metadata (description, topics, language, homepage,
- `suggest_pins(username, limit?)` — Top repos by stars for a user (good candidates to pin).

### habit-tracker

_Habits: add_habit, check_in, streak, today (what's due), habit_report, list_habits._

- `add_habit(name, cadence?, target?)` — Add a habit. cadence: daily or weekly. target = times per period (default 1).
- `archive_habit(habit)` — Archive a habit (keeps history; hides from `today`).
- `check_in(habit, date?, count?, note?)` — Record a check-in for a habit (by name or id). `date`=YYYY-MM-DD (default today).
- `delete_habit(habit)` — Delete a habit and all its check-ins.
- `habit_report(days?)` — Per-habit completion over the last N days: check-in count and best streak.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_habits(status?)` — List habits by status (active/archived).
- `streak(habit)` — Current streak for a habit (consecutive days, or weeks for weekly habits).
- `today()` — Active habits and whether they're satisfied for the current period (today / this week).

### homebrew

_Manage brew packages: search, info, list, outdated, deps, leaves, services, bundle, doctor, cleanup, install(confirm=True)._

- `analytics(on?, confirm?)` — Read or set Homebrew analytics. on=None reads state; setting requires confirm=True.
- `autoremove(dry_run?, confirm?)` — Remove formulae installed only as dependencies that are no longer needed. dry_run previews;
- `bundle_check(path?)` — Check whether everything in a Brewfile is installed (read-only).
- `bundle_dump(path?, force?, confirm?)` — Write a Brewfile snapshot of installed formulae/casks/taps. Writes a file — requires confirm=True.
- `bundle_install(path?, confirm?)` — Install everything listed in a Brewfile. Mutating — requires confirm=True.
- `cleanup(name?, dry_run?, confirm?)` — Remove stale downloads/old versions. dry_run=True (default) only previews. To actually clean,
- `config()` — Show Homebrew's configuration / environment (read-only).
- `deps(name, tree?)` — Show dependencies of a formula. tree=True shows the full dependency tree.
- `doctor()` — Run `brew doctor` to diagnose common issues (read-only).
- `health()` — Liveness check: confirm this server is running and reachable.
- `info(name)` — Show info about a formula/cask.
- `info_json(name)` — Structured info for a formula or cask: versions, deps, homepage, caveats, install state.
- `install(name, cask?, confirm?)` — Install a package. Mutates your system — requires confirm=True.
- `leaves()` — List installed formulae that are not dependencies of another formula (top-level packages).
- `list_casks()` — List installed casks with versions.
- `list_installed()` — List installed formulae with versions.
- `list_taps()` — List the currently tapped repositories.
- `outdated()` — Show outdated packages.
- `pin(name)` — Pin a formula to prevent it from being upgraded (reversible with unpin).
- `search(query)` — Search Homebrew for a formula/cask.
- `services_list()` — List Homebrew-managed background services and their status.
- `services_run(action, name?, confirm?)` — Control a brew service. action: start|stop|restart|run. name empty applies to all (where valid).
- `tap(name?, confirm?)` — Add a tap. Empty name lists current taps (read-only). Adding a tap requires confirm=True.
- `uninstall(name, cask?, force?, confirm?)` — Uninstall a package. Destructive — requires confirm=True. force=True ignores dependents.
- `unpin(name)` — Unpin a previously pinned formula.
- `untap(name, confirm?)` — Remove a tap. Requires confirm=True.
- `update(confirm?)` — Fetch the newest Homebrew package metadata (`brew update`). Requires confirm=True.
- `upgrade(name?, confirm?)` — Upgrade a package (or all if name empty). Requires confirm=True.
- `uses(name, installed?)` — Show formulae that depend on `name` (reverse deps). installed=True limits to what you have installed.

### interview-prep

_SRS flashcards (add_card/due_cards/review, optional review_fsrs) + decks, a DSA tracker (add_problem/track_attempt/tag_problem/pattern_coverage), and a mock-interview scheduler._

- `add_card(front, back, topic?, deck?)` — Add a flashcard (due immediately). Optionally assign a deck.
- `add_cards(items)` — Bulk-add flashcards. Each item: {front, back, topic?, deck?}. Returns created ids.
- `add_problem(title, difficulty?, topic?, url?, pattern?)` — Add a DSA problem to practice. `pattern` is a leetcode pattern tag (e.g. sliding-window).
- `complete_mock(mock_id, score?, notes?)` — Mark a mock interview done with an optional score (0-100) and notes.
- `due_cards(limit?, deck?, topic?)` — Cards due for review now, optionally filtered by deck or topic.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_decks()` — List decks with card counts and how many are due now.
- `list_problems(status?, pattern?, difficulty?, limit?)` — List problems, optionally filtered by status (todo/attempted/solved), pattern, and/or difficulty.
- `mock_calendar_payload(mock_id)` — Return a Google Calendar event payload for a mock interview (pass to google_workspace.create_event).
- `move_card(card_id, deck)` — Move a card to a deck.
- `pattern_coverage()` — Coverage report across the canonical leetcode patterns: which you've practiced vs. gaps.
- `problems_by_pattern()` — Count of problems grouped by leetcode pattern, with solved counts.
- `problems_due(limit?)` — Solved problems scheduled for a spaced redo whose review date has arrived.
- `review(card_id, grade)` — Grade a card 0-5 (SM-2 lite). <3 resets; else interval grows by ease. Schedules next due date.
- `review_forecast(days?)` — How many cards become due on each of the next N days.
- `review_fsrs(card_id, rating)` — Review a card with FSRS (rating: again/hard/good/easy). Modern memory model; opt-in alternative
- `schedule_mock(topic, when_at, duration_min?, kind?)` — Schedule a mock interview. `when_at` is an ISO datetime. kind: technical/behavioral/system-design.
- `set_retention(target)` — Set desired FSRS retention (0.70-0.97; default 0.90). Higher = more frequent reviews.
- `stats()` — Cards due + problems solved/total + mocks (preserved keys, extended).
- `tag_problem(problem_id, pattern?, difficulty?, topic?)` — Set/update a problem's pattern, difficulty, and/or topic.
- `track_attempt(problem_id, solved, review_in_days?)` — Record an attempt; marks solved/attempted. If solved and review_in_days>0, schedules a spaced redo.
- `upcoming_mocks(limit?)` — Upcoming (scheduled) mock interviews, soonest first.

### jobtrack

_Track job applications: add_application, update_status, list_pipeline, due_followups, add_interview, add_note, export_csv/import_csv, analytics._

- `add_application(company, role, jd_url?, status?, followup_in_days?, notes?, contact_name?, contact_email?, location?, salary?, source?, contact_id?)` — Log an application. Sets a follow-up reminder N days out. Optional contact/location/source.
- `add_interview(application_id, kind?, scheduled_at?, notes?, outcome?)` — Log an interview round (kind, optional scheduled time, notes, outcome).
- `add_note(application_id, note)` — Append a timestamped note to an application (preserves existing notes).
- `analytics()` — Funnel + conversion analytics: counts per stage, conversion rates, time-to-offer.
- `archive_application(application_id)` — Soft-archive an application (sets archived_at; keeps the row + history). Reversible.
- `delete_application(application_id)` — Hard-delete an application and its interviews/history (cascade). Errors if it doesn't exist.
- `due_followups(limit?)` — Applications whose follow-up date has passed and aren't closed.
- `export_csv(path, status?)` — Export applications to a CSV file (optionally filtered by status).
- `for_contact(contact_id, limit?)` — List applications linked to a given contacts-server contact_id (P2 cross-server identity).
- `get_application(application_id)` — Full record for one application incl. interviews and status history.
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_csv(path)` — Import applications from a CSV file. Requires at least company,role columns.
- `list_interviews(application_id)` — List interview rounds for an application.
- `list_pipeline(status?, limit?)` — List applications, optionally filtered by status.
- `search(query, limit?)` — Search applications by company, role, location, or notes (substring).
- `set_followup(application_id, in_days?)` — Reschedule the follow-up date N days from now.
- `stats()` — Counts by status.
- `update_status(application_id, status, notes?)` — Move an application to a new stage (wishlist/applied/phone_screen/interview/offer/rejected/accepted).

### learn-tracker

_Track learning: add_course, log_progress, log_study (streaks), set_goal/goal_progress, add_resource, mark_for_review/reviews_due, whats_next, generate_plan, report._

- `add_course(title, provider?, url?, hours?, status?, deadline?, tags?)` — Add a course/tutorial (status: wishlist/in_progress/done). Optional deadline (ISO) and tags.
- `add_resource(course_id, url, kind?, title?, fetch_title?)` — Attach a resource link to a course (kind: link/video/book/article). Fetches page title if absent.
- `current_streak()` — Consecutive days (ending today/yesterday) with at least one study session.
- `generate_plan(hours_per_week?, max_weeks?)` — A week-by-week plan: order unfinished courses (in_progress + nearest deadline first) and bucket by
- `goal_progress()` — Progress toward each active goal in its period.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_courses(status?, tag?)` — List courses, optionally by status and/or tag.
- `list_resources(course_id)` — List resources attached to a course.
- `log_progress(course_id, progress_pct, note?)` — Update a course's progress %. Auto-sets status from the percentage.
- `log_study(minutes, course_id?, note?)` — Log a study session of N minutes (optionally tied to a course). Powers streaks & reports.
- `mark_for_review(course_id, days?)` — Schedule a learned course to resurface for review in N days.
- `report(days?)` — Activity report over the last N days: hours studied, sessions, courses completed, streak.
- `reviews_due(limit?)` — Courses whose spaced-review date has arrived.
- `set_goal(kind, target, period?)` — Set a learning goal. kind: 'minutes' (study time) or 'courses' (completed). period: day/week/month.
- `study_calendar(days?)` — Per-day study minutes for the last N days (heatmap-friendly).
- `whats_next(limit?)` — In-progress courses first (closest to done), then wishlist — what to work on next.

### linkedin-optimizer

_Sharpen LinkedIn copy (draft-only). headline_variants, about_section, experience_bullets, optimize_text, keyword_audit. Uses profile.json._

- `about_section(profile_path?, focus?)` — Draft a LinkedIn About/summary from profile.json (or a given profile path): a hook, what you
- `experience_bullets(role, company, raw)` — Turn raw notes (free text, one idea per line or comma-separated) into polished, strong-verb
- `headline_variants(role?, skills?, focus?)` — Generate and length-check several LinkedIn headline options (limit 220 chars). Falls back to
- `health()` — Liveness check: confirm this server is running and reachable.
- `keyword_audit(text, target_role)` — Check how well text covers recruiter keywords for a target role (e.g. 'backend',
- `list_target_roles()` — List target roles with built-in recruiter keyword sets for keyword_audit.
- `optimize_text(text, kind?)` — Audit any LinkedIn text (kind ∈ {headline, about, post}) for length vs limit, buzzwords/clichés,

### mac-control

_Control macOS: volume, dark_mode, battery, clipboard, notify, screenshot, windows, wallpaper, caffeinate, lock, eject, spotlight, type_text, shortcuts, system info._

- `activate_app(name)` — Bring an application to the foreground (launching it if needed).
- `battery()` — Battery status (percentage + charging state).
- `battery_detailed()` — Detailed battery health: percent, charging state, time remaining, cycle count, condition, max capacity.
- `caffeinate(seconds?, display?, confirm?)` — Prevent sleep using `caffeinate`. seconds=0 keeps awake indefinitely until stop_caffeinate.
- `clear_clipboard()` — Clear the clipboard (write empty text).
- `disk_usage(path?)` — Disk usage for a path (df -h): total, used, available, percent.
- `eject(name?, all?, confirm?)` — Eject a removable volume by name (e.g. 'USB Drive'), or all=True. Requires confirm=True.
- `empty_trash(confirm?)` — Empty the Trash. Destructive — requires confirm=True.
- `frontmost_app()` — Get the name of the frontmost application and its focused window title (if any).
- `get_brightness()` — Read display brightness 0.0-1.0 (needs `brightness` CLI: brew install brightness).
- `get_clipboard()` — Read the clipboard text.
- `get_dark_mode()` — Report whether Dark Mode is currently enabled.
- `get_system_info()` — System overview: macOS version/build, hardware model, CPU, cores, memory (GB), hostname, uptime.
- `get_volume()` — Get current output volume and mute state.
- `health()` — Liveness check: confirm this server is running and reachable.
- `hide_app(name)` — Hide an application's windows (does not quit it).
- `key_stroke(key, modifiers?)` — Send a keystroke (needs Accessibility permission). `key` is a single char or a named key
- `list_shortcuts()` — List the names of your installed Shortcuts (needs the `shortcuts` CLI, built into macOS 12+).
- `list_volumes()` — List mounted volumes under /Volumes with their sizes (df-based).
- `list_windows(app?)` — List window titles. If app is empty, lists windows of the frontmost process.
- `lock_screen()` — Lock the screen immediately (sleeps display + suspends the login session).
- `network_info()` — Current network info: Wi-Fi SSID, local IPv4 on en0, and Wi-Fi power state.
- `notify(title, message, sound?, subtitle?)` — Show a macOS notification (title, message, optional subtitle + sound).
- `open_app(name)` — Open / focus an application by name.
- `open_file(path)` — Open a file/folder with its default app (or Finder).
- `open_url(url)` — Open a URL in the default browser (http/https/mailto/etc — not local file paths).
- `quit_app(name, confirm?)` — Quit an application by name. Mutating — requires confirm=True.
- `run_shortcut(name, input?)` — Run a Shortcut by name, optionally passing text input via stdin. Returns its output.
- `running_apps(visible_only?)` — List running applications with frontmost/visible flags.
- `say(text, voice?, rate?)` — Speak text aloud via the `say` CLI. Optional voice (e.g. 'Samantha') and rate (words/min).
- `screenshot(path?, to_clipboard?, interactive?, window?)` — Capture a screenshot via screencapture. Saves to `path` (defaults to a timestamped PNG in the
- `set_brightness(level)` — Set display brightness 0.0-1.0 (needs `brightness` CLI: brew install brightness).
- `set_clipboard(text)` — Write text to the clipboard.
- `set_dark_mode(on)` — Toggle system Dark Mode.
- `set_focus(mode?, on?)` — Toggle a Focus mode (e.g. 'Do Not Disturb'). macOS has no public Focus CLI, so this runs a
- `set_mute(on?)` — Mute or unmute audio output.
- `set_volume(level)` — Set output volume (0-100). Returns the resulting volume.
- `set_wallpaper(path, confirm?)` — Set the desktop wallpaper to an image file. Mutates appearance — requires confirm=True.
- `sleep(confirm?)` — Put the whole Mac to sleep. Requires confirm=True.
- `sleep_display()` — Put the display to sleep.
- `spotlight_search(query, kind?, limit?, onlyin?)` — Search files via Spotlight (mdfind). `kind` narrows results: app, image, pdf, folder, audio,
- `stop_caffeinate(pid?)` — Stop a caffeinate process started by this server. pid=0 stops all of them.
- `toggle_bluetooth(on)` — Turn Bluetooth on/off (needs `blueutil`: brew install blueutil).
- `toggle_wifi(on)` — Turn Wi-Fi on/off (uses the en0 device by default).
- `type_text(text)` — Type text into the frontmost app via keystroke (needs Accessibility permission).

### mailbox

_Triage Gmail: list_unread / search -> summarize_thread or thread_actions -> draft_reply (drafts only). needs_reply finds what's waiting; summarize_inbox gives an at-a-glance triage. Label/archive/star/trash + Gmail filters supported._

- `apply_label(message_id, label_names, create_missing?)` — Apply labels to a message BY NAME (resolves to IDs; auto-creates missing labels by default).
- `archive(message_id)` — Archive a message (removes it from the INBOX).
- `auth_status()` — Report Gmail auth state for mailbox.
- `batch_modify(message_ids, add_labels?, remove_labels?)` — Bulk add/remove label IDs across many messages in one call (great for mass triage).
- `categorize(message_id, add_labels, remove_labels?)` — Apply/remove Gmail label IDs on a message (e.g. mark read by removing UNREAD).
- `create_filter(from_query?, to_query?, subject?, has_words?, add_label_names?, archive?, mark_read?)` — Create a Gmail filter. Provide criteria (from/to/subject/has_words) and actions: apply labels
- `create_label(name)` — Create a Gmail label (idempotent: returns the existing one if the name already exists).
- `delete_filter(filter_id)` — Delete a Gmail filter by id.
- `download_attachment(message_id, attachment_id, dest_dir?)` — Download a message attachment to disk (defaults to the mailbox data dir). Returns path + bytes.
- `draft_reply(thread_id, to_email, subject, body)` — Create a DRAFT reply within a thread (nothing sent).
- `get_message(message_id, include_body?)` — Full single-message view: parsed from/to/subject/date, body text, and attachment metadata.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_filters()` — List existing Gmail filters (id, criteria, action).
- `list_labels()` — List all Gmail labels (id, name, type, message/thread totals).
- `list_threads(query?, max_results?)` — List threads matching a Gmail query, with message counts and the latest message's from/subject.
- `list_unread(max_results?)` — List unread messages (id, thread, from, subject, snippet).
- `mark_read(message_id)` — Mark a message read (removes the UNREAD label).
- `mark_unread(message_id)` — Mark a message unread (adds the UNREAD label).
- `needs_reply(max_results?)` — Unread messages in the inbox not sent by you — i.e. likely waiting for a reply.
- `remove_label(message_id, label_names)` — Remove labels from a message BY NAME (resolves to IDs; ignores names that don't exist).
- `search(query, max_results?, include_snippet?)` — Search Gmail with any Gmail query operators (e.g. 'from:x newer_than:7d has:attachment is:unread').
- `star(message_id)` — Star a message.
- `summarize_inbox(max_results?)` — At-a-glance triage: counts of unread / needs-reply, top senders, and the oldest unread message.
- `summarize_thread(thread_id)` — Return a thread's messages as plain text so you (the agent) can summarize/extract actions.
- `thread_actions(thread_id)` — Return a thread's text plus a heuristic list of likely action items / open questions per message
- `trash(message_id)` — Move a message to Trash (recoverable for 30 days).
- `unstar(message_id)` — Unstar a message.
- `untrash(message_id)` — Restore a message from Trash.

### mailmerge

_Import (import_csv) -> validate (validate_recipients) -> preview/personalize_check -> dedupe vs your ledger -> throttle_plan -> render_batch, then send via reachout.create_draft per row so caps + the campaign ledger stay authoritative. Never sends directly._

- `campaign_advice(sendable, daily_cap?, cooldown_days?)` — Deterministic guidance on running this batch safely: dedupe-vs-ledger, cooldown, warmup ramp,
- `dedupe(recipients, against_emails?, use_ledger?)` — Collapse duplicate emails within the batch and (optionally) subtract emails already contacted.
- `dry_run(recipients, template_name, daily_cap?)` — Validate a batch without sending: flag missing/invalid emails, duplicate addresses, and whether
- `export_preview(recipients, template_name, common?, fmt?, dest?, limit?)` — Write a human-reviewable preview file (fmt: md | csv | html) of the rendered batch to disk
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_csv(source, email_column?, is_path?)` — Parse a CSV into the recipients shape (list of dicts). `source` is a file path (is_path=True) or
- `personalize_check(recipients, template_name, common?)` — Spam-quality guardrail: detect rows that render IDENTICAL bodies (sign the template isn't actually
- `preview(recipients, template_name, common?, limit?)` — Render the batch (first `limit` rows) so you can eyeball it before anything is created.
- `render_batch(recipients, template_name, common?, limit?)` — Render the full reachout-ready payload for each recipient (to_email/subject/body + recipient_name/
- `throttle_plan(count, daily_cap?, per_minute?, start_iso?, skip_weekends?)` — Compute a humane send schedule for `count` messages: how many days, daily batch sizes, the
- `validate_recipients(recipients, required_vars?)` — Per-row hygiene check (no template needed): valid email syntax, missing required vars, and

### meeting-prep

_Assemble a meeting briefing from read-only suite DBs (contacts, reachout, codeindex, notes). build_brief(company?, attendees?, topic?) and quick_brief(text)._

- `build_brief(company?, attendees?, topic?)` — Assemble a structured meeting brief. Pulls READ-ONLY from contacts (by company/attendee),
- `health()` — Liveness check: confirm this server is running and reachable.
- `quick_brief(text)` — Lightweight brief from a free-text blurb (e.g. a calendar invite). Extracts likely company,

### news-radar

_Find timely outreach signals for a company (free, no key). scan_company(name) pulls Google News + Hacker News; classify hiring/funding/launch/exec triggers. list_signals()/top_signals() browse them._

- `health()` — Liveness check: confirm this server is running and reachable.
- `list_signals(company?, signal?, limit?)` — Browse stored signals, optionally filtered by company and/or signal type (most recent first).
- `scan_company(name, since_days?, limit?, source?)` — Scan recent news/signals for a company by name. source ∈ {all, google_news, hackernews}.
- `signal_types()` — List the signal categories this server detects (offline; no network).
- `stats()` — Summary: total signals stored and breakdown by type/source.
- `top_signals(limit?, exclude_news?)` — Return the most recent actionable signals across all companies (excludes generic 'news' by

### notes

_Markdown notes w/ [[backlinks]], #tags, daily notes, templates + FTS. new_note, edit_note, search, get, backlinks, graph, export_note/export_all, daily_note._

- `append_note(title, text)` — Append text to a note (creates it if missing).
- `append_to_daily(text, date?)` — Append a timestamped line to today's (or `date`) daily note.
- `backlinks(title)` — Notes that link TO this title via [[title]].
- `broken_links()` — [[links]] that point to non-existent notes.
- `daily_note(date?)` — Get-or-create today's (or `date`=YYYY-MM-DD) daily note. Returns its title and body.
- `delete_note(title)` — Delete a note by title.
- `edit_note(title, body)` — Replace a note's body (re-derives tags). Creates the note if it doesn't exist.
- `export_all(dir?)` — Write every note to .md files in a directory (default <data_dir>/exports).
- `export_graph_json(path?)` — Write the backlink graph to a JSON file. Default: <data_dir>/exports/graph.json.
- `export_note(title, dir?)` — Write one note to a real .md file (with YAML frontmatter).
- `find_by_link(target)` — Notes that contain a [[target]] link (alias-friendly view of backlinks, including broken ones).
- `get(title)` — Get a note by title, with its outgoing links and tags.
- `graph()` — Full backlink graph: nodes (titles), edges ([[links]]), and broken links.
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_markdown(dir)` — Import a folder of .md files as notes (filename stem becomes the title; frontmatter stripped).
- `link_suggestions(title, limit?)` — Suggest notes to link to from a note, via FTS over its body (excludes already-linked + self).
- `list_notes(limit?)` — List recent notes.
- `list_tags()` — All tags with note counts.
- `list_templates()` — List saved templates.
- `merge_notes(source, target, delete_source?, update_links?)` — Merge `source` note into `target` (appends its body). Optionally rewrites [[source]] links to
- `new_from_template(title, template, vars?)` — Create a note from a saved template, rendering Jinja2 vars (falls back to literal body).
- `new_note(title, body?, tags?)` — Create a note (unique title). Use [[Other Title]] to link notes and #tags inline. Extra tags
- `notes_by_tag(tag)` — Notes carrying a given tag.
- `orphans()` — Notes with no incoming and no outgoing links.
- `outline(title)` — Extract the markdown heading outline of a note (level + text, in order).
- `recent(days?, limit?)` — Notes updated within the last N days, most recent first.
- `rename_note(old_title, new_title, update_links?)` — Rename a note; optionally rewrite [[old]] links in other notes to [[new]].
- `save_template(name, body)` — Save a reusable note template. Body may contain Jinja2 placeholders like {{ topic }}.
- `search(query, limit?)` — Full-text search notes.
- `stats()` — Counts: notes, tags, links, orphans, broken links, total words.
- `tag_note(title, tags)` — Add tags to a note (comma/space separated; merged with existing & inline #tags).

### pitchbuilder

_Store a tailored project pitch per company and render it into your template. save_pitch -> render_into_template -> (optional) make_onepager -> onepager_to_pdf. suggest_angles for ideas; save_variant for A/B; save_template for a reusable library._

- `delete_template(name)` — Delete a template from the managed library.
- `get_pitch(company)` — Get the stored pitch for a company.
- `get_template(name)` — Get a template from the managed library.
- `get_variant(company, variant)` — Get a specific per-company pitch variant.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_onepagers()` — List generated one-pager / PDF files in the output dir.
- `list_pitches(limit?, tag?)` — List saved pitches, optionally filtered by tag.
- `list_templates(tag?)` — List templates in the managed library, optionally filtered by tag.
- `list_variants(company)` — List all stored pitch variants for a company.
- `make_onepager(company, sections, filename?, tagline?)` — Write a markdown one-pager (problem/solution/why-us/ask) to attach. Returns its path.
- `onepager_to_pdf(md_path)` — Convert a markdown one-pager to PDF: render md -> .docx (python-docx) -> .pdf (LibreOffice).
- `read_profile()` — Return profile.json facts (name, headline, projects, skills, links) to ground a pitch.
- `render_into_template(template, context)` — Render your fixed outreach template (Jinja2 string) with a context dict.
- `render_template(name, context)` — Render a library template (Jinja2) with a context dict. Returns {subject, body}.
- `save_pitch(company, pitch, domain?, angle?, tags?)` — Save (or replace) the tailored project pitch for a company.
- `save_template(name, body, subject?, tags?)` — Store or replace a reusable email/pitch template in the managed library (keyed by name).
- `save_variant(company, variant, pitch, angle?)` — Save a per-company A/B pitch variant (e.g. 'short', 'technical'). Keyed by company+variant.
- `suggest_angles(company?, context?)` — Suggest outreach angles for a company. Rules-based, free, offline — returns a list of

### portfolio-site

_build_site(theme, github_username, out_dir) from profile.json; set_theme; list_themes; projects_from_github; deploy_instructions._

- `build_site(theme?, out_dir?, github_username?, merge_github?, url?, og_image?, description?)` — Generate index.html from profile.json. Optionally pull projects from GitHub (github_username;
- `deploy_instructions(repo?, branch?)` — Return step-by-step free GitHub Pages deploy commands for the generated site.
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_themes()` — Available site themes.
- `projects_from_github(username, limit?)` — Fetch a user's top non-fork repos as portfolio project dicts (name, tagline, link, tech,

### project-memory

_Durable project memory. At session start call resume() to rehydrate the working thread; remember() decisions/conventions as you make them; pin() the load-bearing ones; link() related memories and graph() to traverse; checkpoint() at milestones so context survives compaction. export_digest() writes a committable MEMORY.md._

- `auto_tag(memory_id, project?)` — Derive tags from a memory's note (tech words, identifiers, file paths) and merge them in.
- `checkpoint(summary, open_items?, project?)` — Save the current working thread (what we're doing + what's still open) before the
- `export_digest(project?, write?, target?, prime_clients?)` — Build a committable markdown digest of this project's memory: pinned items, decisions,
- `find_duplicates(project?, threshold?, limit?)` — Find near-duplicate memories via token Jaccard similarity (>= threshold). Returns candidate
- `forget(memory_id)` — Delete a memory by id (also removes its graph edges).
- `forget_where(project?, kind?, tag?, older_than_days?)` — Bulk-delete memories matching a filter (kind / tag substring / age). At least one filter is
- `graph(memory_id, depth?, project?)` — Traverse the relation graph from a memory up to `depth` hops (BFS). Returns the reachable
- `health()` — Liveness check: confirm this server is running and reachable.
- `link(a_id, b_id, project?, rel?)` — Record a typed relationship between two memories (knowledge-graph edge). Common rels:
- `links_of(memory_id, project?)` — Direct neighbors of a memory (both directions) with the relation type and a note preview.
- `list_memories(kind?, project?, limit?, pinned_only?)` — List memories for this project, optionally filtered by kind or pinned-only (pinned + most
- `merge(keep_id, drop_ids, project?)` — Merge duplicate memories into `keep_id`: union their tags, rewire their graph edges to
- `pin(memory_id, pinned?)` — Pin (or unpin) a memory so it always surfaces in resume() and gets a recall boost.
- `recall(query, project?, limit?, kind?, boost_pinned?)` — Full-text search your memories for this project, ranked by a blend of relevance +
- `remember(note, kind?, tags?, project?, importance?, pinned?, auto_tag?)` — Save a durable memory. kind ∈ {decision, todo, glossary, convention, context}. Optionally
- `resume(project?)` — Rehydrate context: the latest checkpoint (with open_items parsed to a list) + pinned and
- `set_importance(memory_id, importance)` — Set a memory's importance (0-5). Higher importance ranks higher in recall and resume.
- `stats(project?)` — Counts of memories by kind + checkpoint/link/pinned counts for this project.
- `timeline(project?, limit?)` — Unified chronological feed of memories + checkpoints (newest first).
- `unlink(a_id, b_id, project?)` — Remove edge(s) between two memories (either direction).
- `update_memory(memory_id, note?, kind?, tags?)` — Edit a memory in place (note/kind/tags). Keeps FTS in sync and stamps updated_at.

### reachout

_Templated Gmail outreach, drafts-first. render_template -> create_draft (review) -> send_draft. Tracks everything; list_followups_due for follow-ups; sequences + scheduled sends + A/B + analytics._

- `ab_pick(template_a, template_b, key)` — Deterministically pick template A or B for a given key (e.g. recipient email) via a stable
- `ab_report()` — Per-variant sent / replied / reply-rate from the tracker.
- `advance_sequence(outreach_id, variables?)` — Advance a contact to the next sequence step if they haven't replied: drafts the next step,
- `analytics(days?)` — Outreach funnel over the last N days: drafted/sent/replied counts, reply rate, avg followups,
- `auth_status()` — Report Gmail auth state + where to put credentials.json.
- `cancel_scheduled(scheduled_id)` — Cancel a pending scheduled action.
- `create_draft(to_email, subject, body, recipient_name?, company?, role?, template_used?, attachments?, attach_onepager?, variant?, sequence_id?, step?, contact_id?)` — Create a Gmail DRAFT (nothing sent) and log it. The safe default path — review, then send_draft.
- `define_sequence(name, steps)` — Define a multi-step sequence. steps = [{template, wait_days, condition?}]; condition defaults
- `delete_draft(draft_id, outreach_id?)` — Delete a previously created Gmail draft. Optionally pass the outreach_id (from create_draft)
- `due_scheduled(limit?)` — Pending scheduled actions whose run_at <= now. The agent calls the named action for each.
- `export_csv(path?)` — Export the outreach tracker to a CSV file (for backup/analysis). Returns path + count.
- `get_sequence(name)` — Get a sequence definition (decoded steps).
- `health()` — Liveness check: confirm this server is running and reachable.
- `history_for_contact(contact_id, limit?)` — All outreach rows linked to a given contacts-server contact_id (P2 cross-server identity).
- `list_followups_due(follow_up_days?, limit?)` — Contacts sent >= N days ago, no reply yet, not snoozed — ready for a follow-up. Each row
- `list_outreach(status?, company?, limit?)` — Browse the outreach tracker, optionally filtered by status/company.
- `list_sequences()` — List defined sequences with their step counts.
- `list_templates()` — List available outreach templates (from ./templates/*.md).
- `log_outreach(recipient_name, recipient_email, company, role?, status?, template_used?, notes?, contact_id?)` — Manually record an outreach (e.g. sent outside the tool) for tracking.
- `mark_replied(outreach_id)` — Mark a contact as replied (drops them from follow-ups).
- `mark_scheduled_done(scheduled_id)` — Mark a scheduled action as completed (after the agent has run it).
- `mark_status(outreach_id, status, notes?)` — Set any status (sent/replied/bounced/closed) + optional note.
- `record_followup(outreach_id)` — Bump the follow-up counter + timestamp after sending a follow-up.
- `record_reply(outreach_id, snippet?)` — Record an inbound reply with an optional snippet (marks replied + logs event).
- `render_template(template_name, variables)` — Fill a template (Jinja2) with variables; returns {subject, body, missing}.
- `render_with_profile(template_name, variables)` — Like render_template but pre-fills sender + signature (+ default link) from profile.json,
- `schedule_send(outreach_id, run_at, action?, payload?)` — Queue an action (e.g. send_draft / advance_sequence) for a contact at run_at (ISO time). This is
- `send_draft(draft_id)` — Send a previously created draft and mark it sent.
- `send_email(to_email, subject, body, recipient_name?, company?, role?, template_used?, attachments?, force?, dry_run?, variant?, contact_id?)` — Send immediately (enforces daily cap + dedupe cooldown). Prefer create_draft + send_draft.
- `sequence_status(outreach_id)` — Where a contact is in its sequence (step, total steps, status).
- `signature_block()` — Build a signature from profile.json (name, headline, links). Used as a default when callers
- `snooze(outreach_id, days)` — Snooze a contact for N days — excluded from list_followups_due until then.
- `start_sequence(name, to_email, recipient_name?, company?, role?, variables?)` — Start a sequence for a contact: drafts the first step (drafts-first) and schedules the next.
- `thread_followup(outreach_id, template_name, variables?)` — Draft a follow-up that keeps the same subject (prefixed 'Re:') and threads onto the original

### readme-changelog

_gen_changelog/release_notes from git; gen_readme from structure; suggest_bump, lint_commits, keep_a_changelog, contributors, badges._

- `badges(repo, owner?, name?)` — Generate common shields.io markdown badges (license/build/version/PRs). Pure strings, no network.
- `contributors(repo, from_ref?, to_ref?)` — Contributors with commit counts (via git shortlog), optionally for a ref range.
- `gen_changelog(repo, from_ref?, to_ref?)` — Categorized changelog of commits between two refs (conventional-commit aware).
- `gen_readme(repo)` — Scan a repo's structure + detect stack to seed a README (returns data + a draft outline).
- `health()` — Liveness check: confirm this server is running and reachable.
- `keep_a_changelog(repo, version?, from_ref?, to_ref?)` — Emit a Keep-a-Changelog 1.1.0 section (Added/Changed/Fixed/Deprecated/Removed/Security).
- `lint_commits(repo, from_ref?, to_ref?)` — Flag commits that don't follow the Conventional Commits spec.
- `release_notes(repo, from_ref, to_ref?)` — Markdown-ready release notes between two refs.
- `suggest_bump(repo, from_ref?, to_ref?)` — Suggest a semver bump from conventional commits since `from_ref` (defaults to latest tag).

### recipes

_Playbooks that RETURN an ordered step plan ({server,tool,args,why}) for the agent to run across the other servers. list_recipes, weekly_outreach, prep_for_company, apply_to_job, daily_briefing, ship_project, cross_search._

- `apply_to_job(jd_text)` — Return a step plan to apply to a job from its description `jd_text`: tailor resume, write a
- `capabilities()` — Discover the whole suite: import every servers/*/server.py and return a {server: [tool names]}
- `cross_search(query, limit?)` — Unified read-only search across the shared SQLite FTS indexes — codeindex (files_fts),
- `daily_briefing()` — Return a step plan for a morning briefing: unread feeds, today's tasks, habits due, calendar,
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_recipes()` — List the available playbooks and one-line descriptions.
- `make_change(task)` — Return a step plan to safely make a code change for `task`: gather context, branch, apply a
- `plan_edits(task, repo?)` — Return an ordered list of concrete edit steps for `task`, grounded in the codeindex.
- `prep_for_company(company)` — Return a research step plan for a target `company`: funding, news, contacts, prior outreach,
- `review_and_pr(task)` — Return a step plan to review a change and open a PR for `task`: gather context, validate-edit,
- `ship_project(repo)` — Return a pre-ship checklist step plan for `repo`: health check, README/changelog refresh,
- `start_here()` — Canonical briefing on how to drive this suite: first steps (index_project, project-memory.resume,
- `suite_guide()` — Alias for start_here(): the canonical 'how to drive this suite' briefing.
- `weekly_outreach()` — Return a step plan for a week of outreach across funding-radar/apollo/email-finder/emailcheck/

### repo-health

_Read-only repo hygiene: health_report, stale_branches, large_files, missing_files, todo_census, commit_cadence, gitignore_check._

- `commit_cadence(repo, days?)` — Commit cadence over a window: total commits, active days, commits/week, last commit age.
- `gitignore_check(repo)` — Sanity-check .gitignore and flag commonly-leaked paths that are tracked (e.g. .env, venv).
- `health()` — Liveness check: confirm this server is running and reachable.
- `health_report(repo)` — Aggregate hygiene report with a 0-100 health score and prioritized recommendations.
- `large_files(repo, min_kb?, top?, tracked_only?)` — Find large files in the working tree (default: only git-tracked files >= min_kb).
- `missing_files(repo)` — Check for presence of common project files (README, LICENSE, .gitignore, CI, etc.).
- `stale_branches(repo, days?)` — List local branches whose last commit is older than N days (candidates for cleanup).
- `todo_census(repo, max_files?, limit?)` — Count TODO/FIXME/HACK/XXX/BUG markers across source files, with locations.

### resume-forge

_Build tailored résumés/cover letters from profile.json. read_profile -> ats_score(jd) -> build_resume(template) / cover_letter(...) -> export. JSON Resume import/export. PDF needs LibreOffice._

- `ats_score(jd, resume_text?)` — Score a résumé against a job description (ATS-style). Uses resume_text if given, else builds
- `build_resume(headline?, summary?, sections?, filename?, template?)` — Render a résumé .docx. `sections` = [{heading, items:[...]}, ...]; if omitted, builds from
- `bullet_strength(bullets)` — Analyze résumé bullets for impact: flags weak/passive verbs, missing metrics, over-length,
- `cover_letter(company, role, body, recipient?, filename?)` — Render a cover letter .docx. `body` is the tailored letter text (agent-written).
- `export_json_resume(filename?)` — Export profile.json to the free JSON Resume standard (jsonresume.org). Enables any free JSON
- `export_markdown(headline?, summary?, filename?)` — Export a plain-markdown résumé from profile.json (great for ATS paste + ats_score input).
- `export_pdf(docx_path)` — Convert a .docx to PDF via LibreOffice (free).
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_json_resume(path_or_json, write_to?)` — Parse a JSON Resume (file path or raw JSON string) into the suite's profile.json shape.
- `keyword_gaps(jd)` — Compare a job description's notable terms against your profile skills to spot gaps to address.
- `list_templates()` — Describe the available résumé layouts for build_resume(template=...).
- `list_versions()` — List generated résumé/cover-letter files.
- `read_profile()` — Return profile.json (name, skills, experience, projects, education) for tailoring.

### rss-reader

_Local RSS/Atom reader (SQLite). add_feed, list_feeds, refresh (needs network), unread, mark_read, search, remove_feed._

- `add_feed(url, title?)` — Subscribe to an RSS/Atom feed by URL. Optional human title (auto-filled on first refresh).
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_feeds()` — List subscribed feeds with item + unread counts.
- `mark_read(item_id)` — Mark a single item as read by id.
- `refresh(feed_id?)` — Fetch new items for one feed (feed_id) or all feeds (feed_id=0). Requires network + feedparser.
- `remove_feed(feed_id)` — Unsubscribe from a feed and delete all its items.
- `search(query, limit?)` — Full-text search item titles + summaries across all feeds.
- `unread(limit?)` — List unread items (newest fetched first) across all feeds.

### scaffold

_new_project(stack, name, dest); list_templates; list_licenses; add_ci; add_license; add_precommit; add_devcontainer; add_makefile; git_init._

- `add_ci(project_dir, stack, name?)` — Add a GitHub Actions CI workflow for the given stack.
- `add_devcontainer(project_dir, stack?, name?)` — Write a .devcontainer/devcontainer.json for the stack family.
- `add_editorconfig(project_dir)` — Write a sensible .editorconfig into a project.
- `add_license(project_dir, license?, author?, year?)` — Write a LICENSE file into an existing project (see list_licenses for ids).
- `add_makefile(project_dir, stack?, name?)` — Write a Makefile with install/test/lint/run targets for the stack family.
- `add_precommit(project_dir, stack?)` — Write a .pre-commit-config.yaml appropriate for the stack family (python/node).
- `git_init(project_dir, initial_commit?, branch?)` — Initialize a git repo in the project (optionally with a first commit on `branch`).
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_licenses()` — List available license ids and a one-line summary of each.
- `list_templates()` — List available stacks and the files each generates.
- `new_project(stack, name, dest, license?, author?, git?, ci?, precommit?, makefile?, devcontainer?, editorconfig?)` — Create a new project of `stack` named `name` under `dest`.

### snippet-vault

_Save/search reusable code: save_snippet, search, get, list_by_lang, update_snippet, import_file/import_dir, export, run_snippet, stats, tags._

- `delete(snippet_id)` — Delete a snippet.
- `detect_language(code, filename?)` — Heuristically detect the programming language of a code blob (extension + content sniffing).
- `export(path, lang?, tag?, format?)` — Export matching snippets to a file. format: 'json' or 'markdown'.
- `get(snippet_id)` — Get a snippet's full code. Increments its usage count.
- `health()` — Liveness check: confirm this server is running and reachable.
- `import_dir(path, glob?, tags?, recursive?, max_files?, max_bytes?)` — Bulk-import source files from a directory. Skips binaries and oversized files.
- `import_file(path, title?, lang?, tags?)` — Import a single source file as a snippet (language auto-detected from extension).
- `list_by_lang(lang?, limit?)` — List snippets, optionally by language.
- `run_snippet(snippet_id, stdin?, timeout?, args?)` — Run an interpreted snippet in a bounded subprocess and capture stdout/stderr.
- `save_snippet(title, code, lang?, tags?, description?)` — Save a snippet (title, code, language, comma-tags, optional description).
- `search(query, limit?)` — Full-text search across title/code/tags.
- `stats()` — Library stats: counts by language, most-used snippets, total.
- `tags()` — Distinct tags with usage counts (parsed from comma-separated tag fields).
- `update_snippet(snippet_id, title?, code?, lang?, tags?, description?)` — Partially update a snippet. Only non-empty fields are changed. Stamps updated_at.

### task-manager

_Todos: add_task (natural-language due ok), complete, list_due/list_tasks, subtasks, projects/tags, recurring tasks, eisenhower(), export_ics. calendar_payload(id) -> google_workspace.create_event._

- `add_subtask(parent_id, title, priority?, due?, notes?)` — Add a subtask under a parent task.
- `add_task(title, priority?, due?, notes?, project?, tags?, recurrence?, importance?)` — Add a task. `due` accepts ISO OR natural language ('tomorrow 5pm', 'next fri', 'in 3 days').
- `agenda(days?)` — Open tasks grouped by due day for the next N days, plus an overdue bucket.
- `bulk_complete(task_ids)` — Complete several tasks at once. Returns per-id results (recurring tasks spawn next occurrences).
- `calendar_payload(task_id, duration_min?)` — Return a Google Calendar event payload for a task (pass to google_workspace.create_event).
- `complete(task_id)` — Mark a task done. If it has open subtasks they are also completed. If recurring, spawns the next
- `completion_report(days?)` — Throughput over the last N days: tasks completed, completions by day, and by project.
- `delete_task(task_id)` — Delete a task and its subtasks.
- `eisenhower(urgent_within_days?)` — Open tasks split into the 4 Eisenhower quadrants. Urgent = due within N days (or high priority);
- `export_ics(status?, project?)` — Export tasks with due dates as an RFC5545 ICS calendar (returns the text).
- `get_task(task_id)` — Fetch a single task by id with all of its fields (returns an error if not found).
- `health()` — Liveness check: confirm this server is running and reachable.
- `list_due(limit?)` — Open tasks with a due date that has passed or is today, soonest first.
- `list_projects()` — List projects with open/total task counts.
- `list_tasks(status?, limit?, project?, tag?, parent_id?)` — List tasks by status (open/done), optionally filtered by project, tag, or parent_id (-1 = any).
- `move_to_project(task_id, project)` — Move a task (and its subtasks) into a project.
- `overdue(limit?)` — Open tasks whose due date is strictly in the past, oldest-overdue first, with days overdue.
- `parse_due(text)` — Parse natural-language due text ('tomorrow 5pm', 'next mon', 'in 3 days', '7/1') into ISO.
- `reopen(task_id)` — Reopen a completed task.
- `search_tasks(query, status?, limit?)` — Search tasks by substring in title or notes. Empty status = any status.
- `set_importance(task_id, important)` — Mark a task important (Eisenhower) or not.
- `snooze(task_id, until)` — Push a task's due date out. `until` accepts natural language or ISO.
- `stats()` — Counts: open/done/overdue, by priority, and projects.
- `subtasks(task_id)` — List subtasks of a task.
- `update_task(task_id, title?, priority?, due?, notes?, project?, tags?, recurrence?)` — Update fields on a task (only non-empty args are applied). `due` accepts natural language.
- `write_ics(path?, status?, project?)` — Write tasks to a .ics file. Default path: <data_dir>/exports/tasks.ics.

### time-tracker

_Track time: start(label)/stop(), log_block, pomodoro, set_time_goal/goal_status, daily_report/report/weekly_report, export_csv._

- `category_breakdown(days?)` — Hours per category over the last N days, descending.
- `daily_report(date?)` — Time logged on a specific day (YYYY-MM-DD, default today), by category and label.
- `delete_block(block_id)` — Delete a block.
- `edit_block(block_id, label?, category?, minutes?, note?)` — Edit a logged block (only non-empty/positive args applied).
- `export_csv(days?)` — Return blocks as CSV text (days=0 = all).
- `focus_streak()` — Longest and current streak of consecutive days with at least one logged block.
- `goal_status()` — Progress toward each time goal in its current period.
- `health()` — Liveness check: confirm this server is running and reachable.
- `log_block(label, minutes, category?, note?)` — Log a completed block of N minutes (when you forgot to start a timer).
- `pomodoro_count(days?)` — How many pomodoros completed in the last N days.
- `pomodoro_done()` — Finish the active pomodoro, recording the focus block; suggests a break length.
- `pomodoro_status()` — Status of the active pomodoro: elapsed, remaining, and whether the work interval is up.
- `productivity_by_hour(days?)` — Hours logged bucketed by hour-of-day (0-23) over the last N days — find your peak focus hours.
- `recent_blocks(limit?)` — Most recent completed blocks.
- `report(days?)` — Time report over the last N days (generalizes weekly_report): totals, sessions, breakdowns.
- `running()` — Show the currently running timer (if any) and its elapsed time.
- `set_time_goal(minutes, category?, period?)` — Set a time goal (minutes per period). period: day/week. Empty category = total time.
- `start(label, category?, note?)` — Start a timer (closes any already-running one first).
- `start_pomodoro(label, work_min?, break_min?, category?)` — Start a pomodoro focus session (closes any running timer). pomodoro_status() shows time left.
- `stop()` — Stop the running timer and record its duration.
- `today()` — Quick overview of today: total hours so far, running timer (if any), and category breakdown.
- `top_labels(days?, limit?)` — Most time-consuming labels over the last N days, with hours and share of total.
- `weekly_report()` — Total time in the last 7 days, broken down by category and label.
- `write_csv(path?, days?)` — Write blocks to a CSV file. Default: <data_dir>/exports/time.csv.
